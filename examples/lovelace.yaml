# Anthbot Genie Card — copy-paste examples.
# Replace `lawn_mower.<your_mower>` with your mower's entity id
# (Developer Tools → States → filter "lawn_mower").

# 1. Minimal — expanded map card.
type: custom:anthbot-genie-card
entity: lawn_mower.<your_mower>

# ---------------------------------------------------------------------------
# 2. Compact variant (380px) — good for a sidebar or a dense dashboard.
# type: custom:anthbot-genie-card
# entity: lawn_mower.<your_mower>
# variant: compact

# ---------------------------------------------------------------------------
# 3. Full options.
# type: custom:anthbot-genie-card
# entity: lawn_mower.<your_mower>
# variant: expanded
# show_dock: true
# invert_y: false                 # flip if the yard renders upside-down
# preferred_services: lawn_mower  # lawn_mower | anthbot_genie
# meters_per_unit: 0.001          # zone vertex units → metres (Genie uses mm)
# error_labels:
#   "12": Blade jammed
#   "7": Bumper triggered
# entities:                       # only if auto-resolution needs help
#   battery_level: sensor.<your_mower>_battery_level
#   map_area: sensor.<your_mower>_map_area

# ---------------------------------------------------------------------------
# 4. Two mowers — one card each; serial scoping keeps them independent.
# type: vertical-stack
# cards:
#   - type: custom:anthbot-genie-card
#     entity: lawn_mower.<your_mower>
#   - type: custom:anthbot-genie-card
#     entity: lawn_mower.<your_second_mower>

# ---------------------------------------------------------------------------
# 5. Polished dark "glass" look via card-mod (requires the card-mod plugin).
#    Dark body + light text, the map kept light with readable overlays, green
#    accent, and a clearly highlighted active zone.
# type: custom:anthbot-genie-card
# entity: lawn_mower.<your_mower>
# variant: expanded
# card_mod:
#   style: |
#     ha-card {
#       background: linear-gradient(160deg, rgba(19,36,28,0.93), rgba(12,22,17,0.95));
#       border: 1px solid rgba(255,255,255,0.08);
#       box-shadow: 0 14px 34px -16px rgba(0,0,0,0.7);
#       --ag-accent: #34c77b;
#       --ag-fg: #eef5f0;
#       --ag-muted: #b9ccc0;
#       --ag-faint: #93a79b;
#       --ag-border: rgba(255,255,255,0.12);
#       --ag-border-strong: rgba(255,255,255,0.22);
#       --ag-surface: rgba(255,255,255,0.05);
#     }
#     .state { color: #eef5f0; }
#     .map .badge, .map .legend { color: #16241d; background: rgba(255,255,255,0.92); }
#     .map .pos-state { color: #44584c; background: rgba(255,255,255,0.85); }
#     .zone-strip .zone.active { background: rgba(52,199,123,0.5); border-color: #34c77b; color: #eafff4; }
